[tool.black]
line-length = 119
target-version = ['py313']
include = '\.pyi?$'

[tool.isort]
profile = "black"
line_length = 119

[tool.mypy]
python_version = "3.13"
ignore_missing_imports = true
exclude = "migrations"
follow_imports = "skip"
disable_error_code = "import-untyped"

